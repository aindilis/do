(define-derived-mode do-notes-list-mode
 emacs-lisp-mode "Do-Notes"
 "Major mode for interacting with .do files.
\\{do-notes-list-mode-map}"
 ;; (define-key do-notes-list-mode-map "\C-tg" 'do-notes-list-)
 )

(provide 'do-notes)
