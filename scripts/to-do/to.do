(Errors with this are
 (there are undefs)
 (formalization is not correct..., it has been joined without deconflicting variable names)
 (we'll want to see if we can't create an entry for every piece
 of text, and then perform auto classification on them, so that
 we can reference them. isn't the sayer index more data storage
 then entry stuff) )